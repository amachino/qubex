# Example system configuration for backend selection and runtime settings.
#
# Notes
# -----
# - system.yaml is a catalog keyed by system_id.
# - backend selects the active backend section per system entry.

64Q-B:
  chip_id: 64Q
  backend: quel3
  quel3:
    endpoint: localhost
    port: 50051
