# Installation

This page describes how to install Qubex.

## Prerequisites

- Python 3.10+ (or the version specified by the project)
- A supported OS (macOS, Linux)

## Install

Use your preferred environment manager. For example:

1. Create and activate a virtual environment.
2. Install the package from source.

## Verify

Run a minimal import in a Python session to confirm the installation.
