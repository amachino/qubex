interface:
  display_name: "Implementation Strategy"
  short_description: "Plan API compatibility and edit scope"
  default_prompt: "Use $implementation-strategy to decide the compatibility boundary and implementation plan before changing runtime or public APIs."
