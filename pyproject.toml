[project]
name = "qubex"
version = "0.4.2"
requires-python = ">=3.9"
dependencies = [
    "jupyter",
    "matplotlib",
    "numpy",
    "scipy",
    "scikit-learn",
    "qutip",
    "pydantic",
    "qctrl-visualizer",
]
